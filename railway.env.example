# ══════════════════════════════════════════════════
# Vendor Red Team Passport — Railway Environment Variables
# Copy these into Railway → Project → Variables
# ══════════════════════════════════════════════════

# ── REQUIRED ───────────────────────────────────────────
# LLM provider key (Featherless.ai — serverless open-weight models)
FEATHERLESS_API_KEY=your-featherless-api-key

# Admin / API auth key
VRTP_API_KEY=CHANGE-ME-use-openssl-rand-hex-32
SECRET_KEY=CHANGE-ME-jwt-signing-secret-min-32-chars

# ── DATABASE ──────────────────────────────────────────
# Railway Postgres plugin — Railway sets DATABASE_URL automatically.
# Use the Railway reference syntax to wire it:
DATABASE_URL=${{Postgres.DATABASE_URL}}

# ── PATHS (Railway ephemeral filesystem) ────────────────
# Reports and run artifacts — use a Railway Volume or S3 for persistence
VRTP_REPORTS_DIR=/app/reports
VRTP_DATA_DIR=/app/data

# ── LLM / EVAL CONFIG ───────────────────────────────
# Default model for evaluations
DEFAULT_MODEL=meta-llama/Llama-3.1-8B-Instruct
VRTP_MAX_CONCURRENT=4
VRTP_TIMEOUT=60

# ── OPTIONAL ──────────────────────────────────────────
LOG_LEVEL=INFO

# Railway injects PORT automatically — do not set manually
# PORT=8000
