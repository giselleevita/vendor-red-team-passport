schema_version: policy.v1
policy_id: strict
version: "1.0.0"
risk_tiers: [high, critical]
data_classifications: [confidential, restricted]
rules:
  required_release_gate: PASS
  max_review_required: 0
  allowed_regression_severity: none
  mandatory_evidence: [passport, manifest]
  approval_duration_days: 30
  reassessment_frequency_days: 7
